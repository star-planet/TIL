#include <iostream>
using namespace std;

int main()
{
	int N, Sum = 0;
	cin >> N;
	Sum = ((1 + N) * N) / 2;
	cout << Sum << '\n';
	cout << Sum * Sum << '\n';
	cout << Sum * Sum;
	return 0;
}